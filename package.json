{
  "name": "dydx-flash-loans",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dydx-flash-loans",
    "create-local": "graph create --node http://localhost:8020/ dydx-flash-loans",
    "remove-local": "graph remove --node http://localhost:8020/ dydx-flash-loans",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dydx-flash-loans",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.85.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
